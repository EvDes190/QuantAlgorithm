# QuantAlgorithm
    Simulating quant Grover's algorithm for resolve f(x) = 1 equation in O(√2^n * π/4) time

## Build:
    - Run build script in "build.sh"

## Run:
    - Run grover.exe
    Options: "q=[qubit number]", "d=[debug true/false]", "a=[answer of f(x) = 1]", f=[output file]
